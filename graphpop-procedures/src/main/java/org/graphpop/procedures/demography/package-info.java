/**
 * Demographic-inference procedures (Phase 5, layer 5).
 *
 * <p>Closed-form and approximate-Bayesian demographic estimators
 * built on the Phase-4 ARG layer. v1 ships
 * {@link org.graphpop.procedures.demography.NeTrajectoryProcedure}
 * (M7), which inverts {@code graphpop.arg.coalescence_rate} into
 * a piecewise-constant population-size history.</p>
 */
package org.graphpop.procedures.demography;
